# /public/images/projects/

Place project screenshot files here.
See /components/PortfolioSection.tsx — the `imagePath` field in the projects array.

Recommended:
  - zouhri-preview.png (dental clinic)
  - vortex-preview.png (vortex digital)

Images are 16:9 or ~1280×720px minimum.
