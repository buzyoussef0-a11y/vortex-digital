# /public/animations/

Place Lottie animation files here.
Filename should match what's referenced in HeroLottie.tsx.

When ready:
1. Add your animation JSON file here (e.g. hero-animation.json)
2. Run: npm install lottie-react
3. In /components/HeroLottie.tsx, uncomment the import and <Lottie> tag
